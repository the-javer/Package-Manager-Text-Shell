#!/bin/bash

read -p "Would you like to clear output? y/n: " yn

if [ $yn = y ]; then
    clear
    ./apt-text-start
elif [ $yn = n ]; then
    ./apt-text-start
else
    echo "Invalid input
"
    ./apt-text-clear
fi

$SHELL
